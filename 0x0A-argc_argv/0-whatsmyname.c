#include <stdio.h>
/**
  * main - Entry point
  * @argc - number of arguments
  * @argv[] - arguments vector
  * Return: Always 0
  */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
