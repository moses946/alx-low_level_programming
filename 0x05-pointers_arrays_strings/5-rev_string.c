#include "main.h"
/**
  * print_rev - prints a string in reverse
  * @s: string to be reversed
  */
void rev_string(char *s)
{
	int i;
	char *t;
	
	for (i = 0; s[i] != '\0'; i++)
	{}
	for (i = i; i >= 0; i--)
	{
		t[i] = s[i];
	}
	*s  = *t;
}
